.vscode
awcc
awccd
compile_commands.json
.aider*
Agents.md
AGENTS.md
.cache
build/
.cache/
imgui.ini
result
# 本机调试时崩过（如 GTK 初始化顺序用错）会掉核心转储，别误提交
core
core.*

# 戴尔官方 AWCC 的界面参考图（14 张截图，9.7MB）：只作 UI 重做的本地对照，不入库——
# 其中 9 张超过 pre-commit 的 500KB 大文件门槛，且是戴尔的界面素材。
# 同名副本另在 ~/图片/AWCC Dell官方UI截图/，索引见 ADAPTATION.md 第九节。
AWCC Dell官方UI截图/

# gettext / 编辑器留下的备份文件
*~

# 打包产物（scripts/package.sh）：deb/rpm/Arch 的中间与输出
*.deb
*.rpm
*.pkg.tar.zst
debian/awcc/
debian/.debhelper/
debian/files
debian/debhelper-build-stamp
debian/*.substvars
packaging/rpm/rpmbuild/

# 参考材料（本地下载的准则网页），与官方 UI 截图同理：不入库
AUR提交准则/
